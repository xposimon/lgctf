#ifndef pqueue_h
#define pqueue_h
#include<string>

template <typename T>
class pqueue
{
public:
    pqueue();
    ~pqueue();
    bool isEmpty() const;
    bool clear();
    bool enqueue(T value);
    ValueType dequeue();
    bool enqueue(ValueType value, double priority);
private:
    T *data;
}


//implementation

template <typename T>
pqueue<T>::pqueue()
{
}

#endif
